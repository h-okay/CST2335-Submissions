<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.HakansAndroidLabs" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/my_primary</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/my_secondary</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/my_statusbar</item>

        <!-- Status bar color. -->
        <item name="android:textColor">@color/my_text</item>

        <item name="android:button">@color/button_color</item>

    </style>

    <style name="Theme.HakansAndroidLabs" parent="Base.Theme.HakansAndroidLabs" />
</resources>